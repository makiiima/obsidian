void printHello() { write(999); }

int sum(int x, int y) { return x + y; }

int main() {
  int a = 5, b = 10, c = -1;
  int result = (a * b) / 2;
  int counter = 2;

  while (counter > 0) {
    counter = counter - 1;
    write(counter);
  }

  if (a < 5) {
    write(1);
  } else if (a >= 5) {
    write(0);
  }
  if (result != 25) {
    write(1);
  } else if (b > a && a <= 5) {
    write(1);
  } else {
    write(0);
  }

  printHello();
  sum(3, 4);

  return 0;
}
